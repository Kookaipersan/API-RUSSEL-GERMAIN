<%- include('partials/header') %>

    <h2>Tableau de bord</h2>
    <p>👤 Utilisateur : <strong>
            <%= user.username %>
        </strong></p>
    <p>📧 Email : <%= user.email %>
    </p>
    <p>📅 Date : <%= date %>
    </p>

    <h3 class="mt-4">Réservations en cours</h3>

    <% if (reservations.length> 0) { %>
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>Client</th>
                    <th>Bateau</th>
                    <th>Catway</th>
                    <th>Date de début</th>
                    <th>Date de fin</th>
                </tr>
            </thead>
            <tbody>
                <% reservations.forEach(res=> { %>
                    <tr>
                        <td>
                            <%= res.clientName || "Inconnu" %>
                        </td>
                        <td>
                            <%= res.boatName || "Inconnu" %>
                        </td>
                        <td>
                            <%= res.catwayId ? res.catwayId.catwayNumber : "Non spécifié" %>
                        </td> <!-- Affichage du catwayNumber -->
                        <td>
                            <%= new Date(res.startDate).toLocaleDateString('fr-FR') %>
                        </td>
                        <td>
                            <%= new Date(res.endDate).toLocaleDateString('fr-FR') %>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
        <% } else { %>
            <p>Aucune réservation en cours.</p>
            <% } %>

                <%- include('partials/footer') %>